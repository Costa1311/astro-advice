meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  steps:
    - name: install and build
      command: npm install && npm run build
run:
  command: npm run start -- -p 3000
  containerPort: 3000
